# PWA Icons

Place your app icons here:

- `icon-192.png` — 192×192 px (required for install prompt)
- `icon-512.png` — 512×512 px (required for install prompt)

Replace the placeholder files with your own for a proper install experience. Use PNG with transparent or solid background; `manifest.json` references these paths.
